{
  "name": "reguide-workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo-app dev",
    "test": "pnpm -r test",
    "test:watch": "pnpm --filter reguide test:watch"
  }
}
