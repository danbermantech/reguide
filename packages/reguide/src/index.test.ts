import { ReguideProvider, useReguide } from './index'

describe('ReguideProvider', () => {
  it('should be defined', () => {
    expect(ReguideProvider).toBeDefined()
  })
})

describe('useReguide', () => {
  it('should be defined', () => {
    expect(useReguide).toBeDefined()
  })
})